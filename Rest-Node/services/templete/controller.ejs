const <%= schemaName %>Service = require('../services/<%= schemaName %>Service.js');

<% if (userSelection.includes('getAllInfo')) { %>
// Get all information
exports.getAllInfo = async (req, res) => {
  try {
    const data = await <%= schemaName %>Service.getAllInfo();
    res.status(200).json(data);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};
<% } %>

<% if (userSelection.includes('getInfoById')) { %>
// Get information by ID
exports.getInfoById = async (req, res) => {
  try {
    const data = await <%= schemaName %>Service.getInfoById(req.params.id);
    res.status(200).json(data);
  } catch (error) {
    res.status(404).json({ error: 'Data not found' });
  }
};
<% } %>

<% if (userSelection.includes('postInfo')) { %>
// Create new information
exports.postInfo = async (req, res) => {
  try {
    const data = await <%= schemaName %>Service.postInfo(req.body);
    res.status(201).json(data);
  } catch (error) {
    res.status(400).json({ error: error.message });
  }
};
<% } %>

<% if (userSelection.includes('updateInfoById')) { %>
// Update information by ID
exports.updateInfoById = async (req, res) => {
  try {
    const data = await <%= schemaName %>Service.updateInfoById(req.params.id, req.body);
    res.status(200).json(data);
  } catch (error) {
    res.status(400).json({ error: error.message });
  }
};
<% } %>

<% if (userSelection.includes('deleteInfoById')) { %>
// Delete information by ID
exports.deleteInfoById = async (req, res) => {
  try {
    await <%= schemaName %>Service.deleteInfoById(req.params.id);
    res.status(204).send();
  } catch (error) {
    res.status(404).json({ error: 'Data not found' });
  }
};
<% } %>

<% if (userSelection.includes('deleteAllInfo')) { %>
// Delete all information
exports.deleteAllInfo = async (req, res) => {
  try {
    await <%= schemaName %>Service.deleteAllInfo();
    res.status(204).send();
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};
<% } %>
